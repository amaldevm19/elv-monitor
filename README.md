## ELV Monitor – V1 Scope

- Windows portable app
- Monitor up to 500 IP devices
- Native ICMP + fallback ping
- Log state changes only
- CSV per device
- Simple status dashboard

V1 success = 24h stable run with 300+ IPs
